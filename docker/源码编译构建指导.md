# 源码编译构建指导

`package.sh` 负责源码编译，将各模块产物归集到 `docker/` 目录；`build.sh` 基于 `docker/` 下的产物构建Docker镜像并打包。两个脚本可在同一环境顺序执行，也可跨环境分开执行。

---

## 一、源码编译打包（package.sh）

### 环境依赖

| 工具 | 版本要求 | 说明 | 环境检查 |
|------|----------|------|----------|
| JDK | 17.0.5+ | 后端编译 | `java -version` |
| Maven | 3.8.8+ | 后端构建 | `mvn -version` |
| Python | 3.11+ | 运行时编译 | `python --version` |
| Node.js | 22.12.0+ | 前端编译 | `node --version` |

### 编译步骤

在 `docker/` 目录下，使用 bash 环境执行编译脚本：

```bash
cd docker
sh package.sh
```

> 本脚本是 bash 脚本，需在 bash 环境下执行。Linux/macOS 可直接执行；Windows 环境请使用 Git Bash 或 WSL，不支持 PowerShell 和 CMD。

### 脚本执行流程

| 步骤 | 操作 | 产物 |
|------|------|------|
| 1/3 后端编译 | `mvn clean package -Dmaven.test.skip=true -U` | studio-manager.jar + studio-service.jar + 配置文件 |
| 2/3 前端编译 | `pnpm install --ignore-scripts` → `pnpm run build` | dist目录打包为 agent-console.tar |
| 3/3 运行时复制 | 复制 agent-runtime 源码、requirements.txt | agent_runtime/ + jiuwen/ + tests/ |

编译完成后，各产物归集到 `docker/` 对应子目录：

```
docker/
├── studio-manager/
│   ├── lib/studio-manager.jar
│   └── config/application-manager.yml, log4j2.xml
├── studio-service/
│   ├── lib/studio-service.jar
│   └── config/application-runtime.yml, log4j2.xml
├── studio-runtime/
│   ├── agent_runtime/
│   ├── jiuwen/
│   ├── tests/
│   ├── requirements.txt
│   └── bin/
└── studio-console/
    ├── agent-console.tar
    └── config/nginx.conf
```

> 若编译与构建跨环境执行，需取消 `package.sh` 中 `package_clean` 的注释，将编译产物打成压缩包并清理中间文件。

---

## 二、制作Docker镜像（build.sh）

### 环境依赖

| 工具 | 版本要求 | 说明 | 环境检查 |
|------|----------|------|----------|
| Docker | 19.03+ | 制作Docker镜像 | `docker --version` |

### 构建步骤

1. （可选）跨环境时，拷贝并解压 `package.tar.gz`：

```bash
tar -zxvf package.tar.gz
```

2. 在 `docker/` 目录下执行构建脚本（同环境需确认已执行 `package.sh`）：

```bash
cd docker
sh build.sh
```

### 脚本执行流程

| 步骤 | 操作 | 基础镜像 |
|------|------|----------|
| 1/5 构建 studio-manager | `docker build` | `eclipse-temurin:17-jre` |
| 2/5 构建 studio-service | `docker build` | `eclipse-temurin:17-jre` |
| 3/5 构建 studio-console | `docker build` | `nginx:1.27` |
| 4/5 构建 studio-runtime | `docker build` | `python:3.11-slim` |
| 5/5 导出打包 | `docker save` → `tar -czvf` | - |

镜像TAG格式：`{服务名}:{VERSION}.{BUILD_TIME}.{BUILD_PLATFORM}`，如 `studio-manager:1.0.0.20260526091150.x86_64`。

### 产物

产物路径：`docker/AgentBuilder.tar.gz`

解压后目录结构：

```
AgentBuilder/
├── image/                          # Docker镜像压缩包
│   ├── studio-manager_*.tar        # Agent管理服务镜像
│   ├── studio-service_*.tar        # Agent运行时服务镜像
│   ├── studio-runtime_*.tar        # Python Agent运行时引擎镜像
│   └── studio-console_*.tar        # 前端控制台镜像（Nginx + Angular）
├── compose/                        # Docker Compose单机部署配置
│   ├── .env.example                # 环境变量模板
│   ├── docker-compose.yml          # 容器编排文件
│   └── config/
│       └── nginx.conf              # Nginx反向代理配置
├── k8s/                            # Kubernetes集群部署配置
│   ├── studio-manager.yaml         # Agent管理服务部署配置
│   ├── studio-service.yaml         # Agent运行时服务部署配置
│   ├── studio-runtime.yaml         # Python运行时引擎部署配置
│   └── studio-console.yaml         # 前端控制台部署配置
```
