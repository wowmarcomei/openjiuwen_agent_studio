kind: Deployment
apiVersion: apps/v1
metadata:
  name: studio-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: studio-service
  template:
    metadata:
      labels:
        app: studio-service
    spec:
      volumes:
        - name: shm-volume
          emptyDir:
            medium: Memory
            sizeLimit: 2Gi
      containers:
        - name: container-studio-service
          image: studio-service:1.0.0.20260526091153.x86_64
          env:
            # ======== [必填] 数据库 ========
            # MySQL: jdbc:mariadb://<MYSQL_IP>:3306/<DB_NAME>?allowMultiQueries=true
            # GaussDB: jdbc:opengauss://<HOST1>:8000,<HOST2>:8000,<HOST3>:8000/<DB_NAME>?targetServerType=master
            - name: spring_datasource_url
              value: ''
            - name: spring_datasource_username
              value: ''
            - name: spring_datasource_password
              value: ''
            # 使用GaussDB时取消注释，URL改为jdbc:opengauss格式
            # - name: spring_datasource_driver_class_name
            #   value: 'com.huawei.opengauss.jdbc.Driver'
            # ======== [必填] Redis ========
            # 单机模式：配置 redis_host、redis_port、redis_password
            # 集群模式：配置 cluster_node_list、redis_password
            # 哨兵模式：配置 redis_host、redis_master_name、redis_sentinel_ports、redis_password
            # Redis服务地址，单机模式为Redis节点IP，哨兵模式为哨兵节点IP
            - name: redis_host
              value: ''
            - name: redis_password
              value: ''
            - name: redis_port
              value: '6379'
            # 集群模式时填写，格式：host1:port1,host2:port2,host3:port3
            - name: cluster_node_list
              value: ''
            # 哨兵模式时填写
            - name: redis_master_name
              value: ''
            # 哨兵模式时填写，格式：port1,port2（与redis_host配合）
            - name: redis_sentinel_ports
              value: ''
            # ======== [必填] 对象存储 ========
            - name: obs_url
              value: ''
            - name: obs_bucket
              value: ''
            - name: obs_staging_bucket
              value: ''
            - name: obs_ak
              value: ''
            - name: obs_sk
              value: ''
            # ======== [选填] HTTPS证书 ========
            # 启用时设server_ssl_enabled=true，并填写证书Base64编码
            - name: server_ssl_enabled
              value: 'false'
            - name: CB_CF_SERVER_KEYSTORE
              value: ''
            - name: CB_CF_TRUST_KEYSTORE
              value: ''
            - name: server_ssl_trust_store_password
              value: ''
            - name: server_ssl_key_store_password
              value: ''
            - name: server_ssl_key_password
              value: ''
            # ======== 服务间调用地址（默认无需修改） ========
            # 鉴权配置，默认调自身，HTTPS启动需要改成https
            - name: user_auth_endpoint
              value: 'http://studio-service:31113'
            # studio-manager调用地址，HTTPS启动需要改成https
            - name: agent_manager_endpoint
              value: 'http://studio-manager:31111'
            # studio-runtime调用地址，HTTPS启动需要改成https
            - name: jiuwen_base_url
              value: 'http://studio-runtime:31014'
            # ======== [选填] koosearch服务 ========
            - name: koosearch_endpoint
              value: ''
          resources:
            limits:
              cpu: '2'
              memory: 4Gi
            requests:
              cpu: '2'
              memory: 4Gi
          volumeMounts:
            - name: shm-volume
              mountPath: /dev/shm
          livenessProbe:
            tcpSocket:
              port: 31113
            initialDelaySeconds: 5
            timeoutSeconds: 30
            periodSeconds: 5
            successThreshold: 1
            failureThreshold: 12
          startupProbe:
            tcpSocket:
              port: 31113
            initialDelaySeconds: 5
            timeoutSeconds: 30
            periodSeconds: 5
            successThreshold: 1
            failureThreshold: 240
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: Never
          securityContext:
            privileged: true
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: default-secret
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 60
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - studio-service
                topologyKey: failure-domain.beta.kubernetes.io/zone
            - weight: 40
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - studio-service
                topologyKey: kubernetes.io/hostname
      schedulerName: default-scheduler
      tolerations:
        - key: node.kubernetes.io/not-ready
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 30
        - key: node.kubernetes.io/unreachable
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 30
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600

---
kind: Service
apiVersion: v1
metadata:
  name: studio-service
  namespace: default
spec:
  ports:
    - name: cce-service-0
      protocol: TCP
      port: 31113
      targetPort: 31113
      nodePort: 31113
  selector:
    app: studio-service
  type: NodePort