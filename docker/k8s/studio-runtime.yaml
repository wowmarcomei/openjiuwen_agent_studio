kind: Deployment
apiVersion: apps/v1
metadata:
  name: studio-runtime
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: studio-runtime
  template:
    metadata:
      labels:
        app: studio-runtime
    spec:
      volumes:
        - name: shm-volume
          emptyDir:
            medium: Memory
            sizeLimit: 2Gi
      containers:
        - name: container-studio-runtime
          image: studio-runtime:1.0.0.20260526091150.x86_64
          env:
            # ======== [必填] Redis ========
            # 单机模式：配置 REDIS_HOST、REDIS_PORT、REDIS_PASSWORD
            # 集群模式：配置 REDIS_CLUSTER_NODES、REDIS_PASSWORD、REDIS_MODE=cluster
            # 哨兵模式：配置 REDIS_SENTINEL_MASTER、REDIS_SENTINEL_NODES、REDIS_PASSWORD、REDIS_MODE=sentinel
            - name: REDIS_HOST
              value: ''
            - name: REDIS_PASSWORD
              value: ''
            - name: REDIS_PORT
              value: '6379'
            # 集群模式时填写，格式：host1:port1,host2:port2,host3:port3
            - name: REDIS_CLUSTER_NODES
              value: ''
            # 哨兵模式时填写
            - name: REDIS_SENTINEL_MASTER
              value: ''
            # 哨兵模式时填写，格式：host1:port1,host2:port2,host3:port3
            - name: REDIS_SENTINEL_NODES
              value: ''
            # 单机填single，集群填cluster，哨兵填sentinel
            - name: REDIS_MODE
              value: 'single'
            # Redis数据库编号，默认0
            - name: REDIS_DATABASE
              value: '0'
            # ======== [必填] 对象存储 ========
            - name: DATASOURCE_OBS_SERVER
              value: ''
            - name: DATASOURCE_OBS_BUCKET
              value: ''
            - name: DATASOURCE_OBS_AK
              value: ''
            - name: DATASOURCE_OBS_SK
              value: ''
            - name: DATASOURCE_OBS_ENABLE_SSL
              value: 'false'
            # ======== 服务间调用地址（默认无需修改） ========
            # 模型路由接口地址，HTTPS启动需要改成https
            - name: MODEL_ROUTER_API
              value: 'http://studio-service:31113/v1/agent-builder'
            # ======== [选填] HTTPS证书 ========
            # 启用时设HTTPS=true，并填写证书Base64编码
            - name: HTTPS
              value: 'false'
            - name: HTTPS_CERT_FILE
              value: ''
            - name: HTTPS_KEY_FILE
              value: ''
            - name: TLS_CERT_KEY_PASSWD
              value: ''
            - name: TLS_CERT_KEY_PATH
              value: '/opt/cloud/agent-runtime/tls/key.pem'
            - name: TLS_CERT_PATH
              value: '/opt/cloud/agent-runtime/tls/cert.pem'
            # ======== [选填] 沙箱配置 ========
            # 远程沙箱地址（配置后启用沙箱模式，为空则沙箱不可用自动走 local）
            - name: SECURITY_SANDBOX_SERVER
              value: ''
            - name: SECURITY_SANDBOX_SSL_VERIFY
              value: 'false'
            - name: SECURITY_SANDBOX_IDLE_TTL
              value: '600'
            - name: SECURITY_SANDBOX_TIMEOUT
              value: '300'
            - name: SECURITY_SANDBOX_SCOPE
              value: 'system'
            # ======== [选填] OpenSearch（启用记忆库时必填） ========
            # 格式: host:port，多节点用逗号分隔
            - name: OPENSEARCH_HOSTS
              value: ''
            - name: OPENSEARCH_USERNAME
              value: 'admin'
            - name: OPENSEARCH_PASSWORD
              value: 'admin'
            - name: OPENSEARCH_USE_SSL
              value: 'false'
            - name: OPENSEARCH_SSL_VERIFY
              value: 'false'
            # ======== [选填] 记忆库配置（默认关闭） ========
            # 总开关，设为true启用记忆库功能
            - name: MEMORY_ENABLED
              value: 'false'
            - name: LONG_TERM_MEMORY_ENABLE
              value: 'false'
            - name: MEMORY_USER_PROFILE_ENABLE
              value: 'false'
            # 记忆库抽取用的Embedding模型配置（启用记忆库时必填）
            - name: MEMORY_EMBEDDING_MODEL
              value: 'bge-m3'
            - name: MEMORY_EMBEDDING_BASE_URL
              value: ''
            - name: MEMORY_EMBEDDING_API_KEY
              value: ''
            # 记忆库抽取用的LLM模型配置（启用记忆库时必填）
            - name: MEMORY_LLM_MODEL
              value: ''
            - name: MEMORY_LLM_BASE_URL
              value: ''
            - name: MEMORY_LLM_API_KEY
              value: ''
            # ======== [选填] 记忆库MySQL存储（启用记忆库时必填） ========
            # 记忆库文本数据存储，可与主库共用同一MySQL实例，使用独立数据库
            - name: MEMORY_DB_HOST
              value: ''
            - name: MEMORY_DB_PORT
              value: '3306'
            - name: MEMORY_DB_USER
              value: ''
            - name: MEMORY_DB_PASSWORD
              value: ''
            - name: MEMORY_DB_NAME
              value: 'agent-memory'
            # ======== [选填] 其他配置 ========
            - name: PORT
              value: '31014'
            - name: PLUGIN_SSL_API_CERT
              value: 'false'
            - name: WORKFLOW_EXECUTE_TIMEOUT
              value: '900'
            - name: IR_LLM_API_KEY
              value: 'sk-placeholder'
            - name: JIUWEN_LOG_FILE
              value: '/opt/cloud/logs/tgf_python.log'
            - name: JIUWEN_LOG_PATH
              value: '/opt/cloud/logs/jiuwen.log'
            - name: JIUWEN_LOGGING_LOG_FILE
              value: '/opt/cloud/logs/jiuwen_python.log'
            - name: LOGGING_LOG_PATH
              value: '/opt/cloud/logs/'
            - name: TGF_LOG_DIR
              value: '/opt/cloud/logs/'
          resources:
            limits:
              cpu: '2'
              memory: 4Gi
            requests:
              cpu: '2'
              memory: 4Gi
          volumeMounts:
            - name: shm-volume
              mountPath: /dev/shm
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 31014
              # https启动时，需要改成https
              scheme: HTTP
            initialDelaySeconds: 10
            timeoutSeconds: 1
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 5
          startupProbe:
            tcpSocket:
              port: 31014
            initialDelaySeconds: 5
            timeoutSeconds: 30
            periodSeconds: 5
            successThreshold: 1
            failureThreshold: 240
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: Never
          securityContext:
            privileged: true
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: default-secret
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 60
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - studio-runtime
                topologyKey: failure-domain.beta.kubernetes.io/zone
            - weight: 40
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - studio-runtime
                topologyKey: kubernetes.io/hostname
      schedulerName: default-scheduler
      tolerations:
        - key: node.kubernetes.io/not-ready
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 30
        - key: node.kubernetes.io/unreachable
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 30
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600

---
kind: Service
apiVersion: v1
metadata:
  name: studio-runtime
  namespace: default
spec:
  ports:
    - name: cce-service-0
      protocol: TCP
      port: 31014
      targetPort: 31014
  selector:
    app: studio-runtime
  type: ClusterIP
